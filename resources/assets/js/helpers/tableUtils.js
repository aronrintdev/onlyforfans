export const encodeQueryFilters = (value = '') => {

  // %TODO
  /*
      let params = {
        type: [], // txn_type
        resource_type: [],
      }
      for ( let s of this.txnFilters.txn_type ) {
        if ( s.is_active ) {
          params.type.push(s.key)
        }
      }
      for ( let s of this.txnFilters.resource_type ) {
        if ( s.is_active ) {
          params.resource_type.push(s.key)
        }
      }
      return params
      */
}

export default {
  encodeQueryFilters,
}
